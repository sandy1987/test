%h2.text-center Work Schedules
%table.schedules
  %thead
    %tr
      %th Date
      %th Location
      %th Role
      %th Shift type
      %th Time slot 1
      %th Time slot 2
      %th Total hours
      %th Amount
      %th User
      %th{:colspan => "3"}
  %tbody
    - @work_schedules.each do |work_schedule|
      %tr
        %td= work_schedule.start_date.try(:to_date)
        %td= work_schedule.location
        %td= work_schedule.role
        %td= work_schedule.shift_type
        %td= work_schedule.time_slot_1
        %td= work_schedule.time_slot_2
        %td= work_schedule.total_hours
        %td
          = work_schedule.amount
          $
        %td= work_schedule.user.try(:name)
        %td
          = link_to 'Edit', edit_work_schedule_path(work_schedule)
          |
          \#{link_to 'Destroy', work_schedule, method: :delete, data: { confirm: 'Are you sure?' }}
%br/
= link_to 'New Work Schedule', new_work_schedule_path
